import 'package:flutter/material.dart';

class AdminView extends StatelessWidget {
  const AdminView() : super();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('admin view'),
    );
  }
}
